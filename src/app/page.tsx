import { AuthenticationMethods } from "@/components/Authentication";

export default function Home() {
  return (
    <main className="screen bg-gray-100 flex items-center justify-center">
      <AuthenticationMethods/>
    </main>
  );
}
